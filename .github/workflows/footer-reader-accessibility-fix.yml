name: Footer Reader Accessibility Fix

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  apply-and-verify:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Source corrections already applied
        run: |
          echo "The controlled source correction already ran successfully on this branch."
          echo "This workflow is retained only as branch-local QA history and will not be promoted to main."
