name: Version Production Assets

on:
  push:
    branches:
      - rendered-corrections-2026-09-12
    paths:
      - 'scripts/version-production-assets.py'
      - '.github/workflows/version-production-assets.yml'

permissions:
  contents: write

jobs:
  version-assets:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: rendered-corrections-2026-09-12
          fetch-depth: 0

      - name: Apply versioned production asset URLs and hero motion
        run: python3 scripts/version-production-assets.py

      - name: Commit versioned production references
        run: |
          git config user.name "Sunday-Co"
          git config user.email "hello@sundayandcompany.co"
          git add '*.html' '*/index.html' '*/*/index.html' assets/site.js || true
          if git diff --cached --quiet; then
            echo "No production references needed updating."
            exit 0
          fi
          git commit -m "Version final shared assets and enable hero motion"
          git push origin HEAD:rendered-corrections-2026-09-12
